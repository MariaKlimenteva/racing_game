#include <iostream>

#include "game.h"
//--------------------------------------------------------------------------
int main() 
{
    Game Races;
    
    SDL_SetMainReady();
    // sdl sdl;
    // window window;
    // SDL_Renderer *renderer = nullptr;
    // window.loop(renderer);

                // Initialize();
            
                // while(true) {
                //     Events();
                //     Loop();
                //     Render();
                // }
                // Cleanup();

    return Races.Execute();
}